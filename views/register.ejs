<!DOCTYPE html>
<html>
<head>
  <title>Register - CampusCircle Mart</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <nav>
  <h1>🏫 CampusCircle Mart</h1>
  <div>
    <a href="/">Home</a>
    <% if (user) { %>
      <a href="/my-listings">My Listings</a>
      <a href="/listings/new">Sell Item</a>
      <span>👋 <%= user.name %></span>
      <a href="/logout">Logout</a>
    <% } else { %>
      <a href="/login">Login</a>
      <a href="/register">Register</a>
    <% } %>
  </div>
</nav>

  <div class="form-container">
    <h2>📝 Create Account</h2>
    <% if(error) { %><p style="color:red"><%= error %></p><% } %>
    
    <form method="POST">
      <input name="name" placeholder="Rahul Sharma" required>
      <input name="email" type="email" placeholder="rahul@college.com" required>
      <input name="password" type="password" placeholder="password123" required>
      <input name="course" placeholder="BTech CSE">
      <input name="year" type="number" placeholder="2026">
      <button type="submit">Register</button>
    </form>
    
    <p>Already have account? <a href="/login">Login →</a></p>
  </div>
</body>
</html>
