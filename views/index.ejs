<!DOCTYPE html>
<html>
<head>
  <title>CampusCircle Mart</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <nav>
  <h1>🏫 CampusCircle Mart</h1>
  <div>
    <a href="/">Home</a>
    <% if (user) { %>
      <a href="/my-listings">My Listings</a>
      <a href="/listings/new">Sell Item</a>
      <span>👋 <%= user.name %></span>
      <a href="/logout">Logout</a>
    <% } else { %>
      <a href="/login">Login</a>
      <a href="/register">Register</a>
    <% } %>
  </div>
</nav>

  <section class="hero">
    <div class="hero-text">
      <h2>Buy & sell within your campus</h2>
      <p>Pre‑loved books, electronics, and hostel essentials from verified students. No delivery fees, just quick campus meetups.</p>
      <div class="hero-actions">
        <a href="/listings/new" class="btn-primary">Sell an item</a>
        <a href="#listings" class="btn-secondary">Browse listings</a>
      </div>
    </div>
  </section>

    <section class="trust-strip">
    <span>✅ Only verified students</span>
    <span>💸 Meet on campus, pay via UPI/cash</span>
    <span>♻️ Reduce waste by reusing books & gadgets</span>
  </section>



  <main>
         <form method="GET" action="/" class="search-bar">
  <input
    type="text"
    name="q"
    placeholder="Search by title, category, or description..."
    value="<%= typeof q !== 'undefined' ? q : '' %>"
  />
  <select name="category">
    <option value="All" <%= (category === 'All' || !category) ? 'selected' : '' %>>All categories</option>
    <option value="Electronics" <%= category === 'Electronics' ? 'selected' : '' %>>Electronics</option>
    <option value="Books" <%= category === 'Books' ? 'selected' : '' %>>Books</option>
    <option value="Furniture" <%= category === 'Furniture' ? 'selected' : '' %>>Furniture</option>
    <option value="Clothes" <%= category === 'Clothes' ? 'selected' : '' %>>Clothes</option>
    <option value="Other" <%= category === 'Other' ? 'selected' : '' %>>Other</option>
  </select>
  <button type="submit">Search</button>
</form>



       <h2 id="listings">📦 Latest Listings</h2>

    <% if(listings.length === 0) { %>
      <p>No listings yet. <a href="/register">Create account</a> to start selling!</p>
    <% } else { %>
      <div class="listings-grid">
        <% listings.forEach(listing => { %>
          <div class="listing-card">
            <h3><a href="/listings/<%= listing.id %>"><%= listing.title %></a></h3>
            <p><strong>₹<%= listing.price %></strong> • <%= listing.category %> • <%= listing.item_condition %></p>
            <small>by <%= listing.seller_name %></small>
          </div>
        <% }) %>
      </div>
    <% } %>

        <section class="story">
      <h3>Why CampusCircle Mart?</h3>
      <p>Every semester, students struggle to buy and sell used items through random groups or expensive delivery apps.</p>
      <p>CampusCircle Mart connects only verified students so you can safely trade within your college—saving money and reducing waste.</p>
    </section>


        <section class="perks">
      <h3>Student perks</h3>
      <ul>
        <li>No commission or listing fees for students.</li>
        <li>Highlight eco‑friendly trades (reuse and donate items).</li>
        <li>Discover free / giveaway items under ₹0 listings.</li>
      </ul>
    </section>

  </main>
  
    <footer class="footer">
    <p>Need help? Contact the CampusCircle Mart team at <span>support@yourcollege.edu</span></p>
  </footer>

</body>
</html>
