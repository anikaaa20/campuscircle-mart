<!DOCTYPE html>
<html>
<head>
  <title><%= listing.title %> - CampusCircle Mart</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
   <nav>
  <h1>🏫 CampusCircle Mart</h1>
  <div>
    <a href="/">Home</a>
    <% if (user) { %>
      <a href="/my-listings">My Listings</a>
      <a href="/listings/new">Sell Item</a>
      <span>👋 <%= user.name %></span>
      <a href="/logout">Logout</a>
    <% } else { %>
      <a href="/login">Login</a>
      <a href="/register">Register</a>
    <% } %>
  </div>
</nav>


  
  <main style="max-width: 800px; margin: 2rem auto;">
    <div class="listing-card" style="padding: 2rem;">
      <% if(listing.images && listing.images.length > 0) { %>
        <img src="/uploads/<%= listing.images[0] %>" style="width: 100%; max-width: 500px; border-radius: 8px;">
      <% } %>
      
      <h1><%= listing.title %></h1>
      <h2 style="color: #10b981;">₹<%= listing.price %></h2>
      
     <div class="detail-meta-box">
  <p><strong>Category:</strong> <span><%= listing.category %></span></p>
  <p><strong>Condition:</strong> <span><%= listing.item_condition %></span></p>
  <p><strong>Seller:</strong> <span><%= listing.seller_name %></span></p>
</div>

      
      <p class="detail-description"><%= listing.description %></p>

      <div style="margin-top: 2rem;">
        <button style="background: #3b82f6; color: white; padding: 1rem 2rem; border: none; border-radius: 8px; font-size: 18px;">
          💬 Contact Seller
        </button>
      </div>
    </div>
  </main>
</body>
</html>
